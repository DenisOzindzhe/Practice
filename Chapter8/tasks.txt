
Как получить адрес переменной?

Как присвоить значение указателю?

Как создать новый указатель?

- для того чтобы получить адресс в памяти переменной стоит использовать специальный символ * перед типом, что вернет
указатель на место в памяти для нее прим.*int *float64

- чтобы присвоить значение указателю используется символ & перед названием переменной на которую мы указываем
somemodif(&x) - присвоит указателю типа переменной x ссылку на место в памяти для типа х

- чтобы создать новый указатель используется оператор new - что выделяет место в памяти для указателя, x := new(int)
указатель для переменной х, типа  int.